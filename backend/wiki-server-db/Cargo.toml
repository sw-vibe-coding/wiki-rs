[package]
name = "wiki-server-db"
version = "0.1.0"
edition = "2024"

[dependencies]
wiki-common = { path = "../../shared/wiki-common", features = ["server"] }
rusqlite = { version = "0.32", features = ["bundled"] }
tokio = { version = "1", features = ["full"] }
